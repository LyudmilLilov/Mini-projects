import time
from plyer import notification

if __name__ == "__main__":
    while True:
        notification.notify(
            title = "New message!",
            message = "You have a new message in chat!",
            timeout = 10
        )
        time.sleep(3600)
